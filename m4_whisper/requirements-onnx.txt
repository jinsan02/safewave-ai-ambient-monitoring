# Separate environment from the team's pinned ai/requirements.txt.
numpy==2.2.6
soundfile==0.14.0
onnxruntime==1.23.2
transformers==4.57.6
optimum==2.1.0
optimum-onnx==0.1.0
huggingface-hub==0.36.0
onnx==1.19.1
torch==2.13.0
# CPU inference. Install torch from the CPU wheel index first to avoid CUDA extras.
