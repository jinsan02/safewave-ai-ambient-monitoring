# M3 v34 병합·실측 공유 (2026-09-23, 소민섭 님께)

작성: 노진산(통합). `feature/ast-base`를 develop에 병합했습니다(`cb0ea6f`, 원격 반영).

## 1. 병합하면서 바꾸거나 보류한 것
| 항목 | 처리 | 이유 |
|---|---|---|
| M3 호출 위치 | develop 구조 유지: M3는 **오디오 워커**에서 VAD 이벤트 1건(최대 6초)마다 실행. 워커가 `{"waveform","sample_rate","raw_peak"}` dict로 넘김 | develop은 M3를 CSI 루프에서 뺐음. 브랜치의 `_attach_env_sound_timing`·CSI 루프 병합 코드는 같은 기능이 이미 있어 제외 |
| `M3_AUDIO_MERGE_MS` | 쓰지 않음 | 워커가 이벤트 하나(≤6초)를 그대로 넘기고 3초 선택은 M3(`peak`)가 함 |
| ORT 스레드 | `get_session_opts(M3_ORT_THREADS)` 유지(RPi5 compose 기본 1) | RPi5에서 M1·sensing과 코어를 나눠 쓰기 위한 기존 제한 |
| VAD 기본값 | **-45 dB 유지**(브랜치 -55 보류) | 노트북 마이크에서 -45로도 먼 대화·잡음 조각이 많아 M4 환각이 늘었음. -55는 RPi5 실측 후 결정 |
| requirements | develop 유지(브랜치의 torch 2.2.2·transformers 4.38.2 고정 제외) | M3 ONNX는 torch 불필요, M4는 transformers 4.46 이상 필요 |
| `EXPERT_INFER_TIMEOUT_MS` | 1000 유지 | M3는 실행기 타임아웃을 안 탐(오디오 워커 직접 호출) |
| sensing 게인 보정·`raw_peak` 메타 | 그대로 반영 | — |

## 2. 확인 결과 (노트북)
| 항목 | 결과 |
|---|---|
| 계약 검증 `setup_m3_ast_onnx.py` | PASS, sha256 `d06265e9…b615` 일치 |
| 단위 테스트 `test_m3_onnx` | 17개 통과 |
| 경계값 | 게이트 0.0049→silence / 0.005·0.0051→모델, 충격 0.5999→알림 없음 / 0.6·0.6001→알림, 게이트 걸리면 충격 0.99도 알림 없음 — 모두 기대대로 |
| 실제 음성 | 녹음한 "도와주세요"를 5분마다 주입(22회): **speech 21회(확률 0.999), impact 확률 최대 0.0005**. 1회는 Phase 2 중이라 설계대로 M3 생략 |
| 5분 파이프라인 | 주입 20/20 처리, 라벨 silence·unknown(빈 방 배경) |

## 3. 속도·메모리
| 환경 | M3 1회(3초 창) |
|---|---|
| 노트북 CPU 1스레드(RPi5 설정과 같음) | **2.15 s** |
| 노트북 CPU 8스레드 | 0.61 s |
| 노트북 GPU(RTX 5060, CUDA EP) | **0.04 s** |
| RPi5 | 아직 미측정 — 1스레드면 6~8 s로 추정 |

- 이전 AST(9/17 RPi5 이벤트→결과 6.3 s)보다 CPU에서 약 2배 무겁습니다. RPi5에서 스레드 1·2·4를 재서 정할 예정입니다. 권장 스레드 수가 있으면 알려 주세요.
- ai-experts 메모리가 **2.71 GB / 한도 3 GB(90%)**로 올라갔습니다(M1~M4 합계). RPi5도 한도가 같아 여유가 적습니다.

## 4. 참고
- Phase 2(음성 응답 대기) 중에는 설계상 M3를 건너뜁니다.
- 우리 쪽 테스트 순서 문제: `test_m5_pipeline` 뒤에 `test_m3_onnx`를 돌리면 가짜 `utils` 모듈 때문에 실패합니다. 기본 순서(파일명순)에서는 통과합니다. 통합 쪽에서 정리하겠습니다.

## 5. 부탁
1. **이 실험실에서 충격음 실측**(물건 떨어뜨리기·문 닫기·매트 낙상)을 영상 촬영 때 함께 하고 싶습니다. 원하는 조건이 있으면 알려 주세요.
2. RPi5 권장 스레드 수, 모델 로드 후 메모리(문서의 0.57 GB가 1스레드 기준인지).
3. VAD -55 dB가 필요한 근거(원거리 충격음 감도)가 있으면 공유 부탁드립니다. RPi5에서 -45/-55를 비교하겠습니다.

## 6. 평가 데이터셋 수령·재현 (09-24 00:10)
받은 `safewave_eval_core.zip`, `safewave_eval_ambient_20260911.zip`을 노트북 `data/m3_eval_v34/`(Git 제외)에 풀었습니다. zip 무결성·파일 수 README와 일치.

| 확인 | 결과 |
|---|---|
| 형식 | 전부 16 kHz mono, **16비트 PCM**(README에는 float32로 적혀 있음 — 동작 영향 없음) |
| 파일 | 낙상 50(10초), 오탐 사례 16(5초), 저음 발화 3, ambient 48(4.00시간) |

낙상 50회 재현(통합 모듈 `EnvSoundAnalysisModel`, 임계 0.6, CPU):

| 장소 | 슬라이딩 3초·1초 홉 | 운영 경로(클립 전체 → peak 3초) | `fall_test/results.csv` |
|---|---|---|---|
| 메인방 | 10/10 | 10/10 | 9/10 |
| 부엌 문닫힘 | 10/10 | 10/10 | 8/10 |
| 부엌 문열림 | 10/10 | 10/10 | 10/10 |
| 화장실 문닫힘 | 7/10 | 6/10 | 6/10 |
| 화장실 문열림 | 10/10 | 10/10 | 6/10 |
| 합계 | 47/50 | **46/50** | 39/50 |

- 운영 경로 46/50이 문서의 보고값과 같습니다 → 병합한 통합 경로가 의도대로 동작.
- `results.csv`는 기록 시각이 2026-08-13이라 **이전 모델 결과**로 보입니다(우리 슬라이딩 max_impact와 평균 0.22 차이). v34로 다시 채점한 CSV가 있으면 교체 부탁드립니다.
- 다음: ambient 4시간으로 오탐률(FA/h)을 우리 경로에서 재현(GPU 약 10분) → `scores_v34_homepos_20260911.csv`와 대조.
- 재현 스크립트: `scripts/dev/m3_fall_repro.py`.
