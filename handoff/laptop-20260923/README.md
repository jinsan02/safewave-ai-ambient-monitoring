# 09-23 노트북 실험 인계 — 요약과 팀원별 할 일

작성: 노진산(통합) · 2026-09-24 · 코드 develop(원격 반영)
상세: 담당자별 문서(`TO_*.md`, `M5_NOTES_NOJINSAN.md`), 0값 통계 `csi_zero_stats_20260923_2347.xlsx`.
노트북 전체 기록(`reports/laptop/EXPERIMENT_CPU_GPU.md`)은 Git 제외 — 공유 폴더·노션에 별도 첨부.

## 1. 오늘 한 일
| 항목 | 결과 |
|---|---|
| 환경 | 실제 ESP32 4노드 → 노트북(192.168.1.11, ipTIME 고정 할당), USB 마이크·노트북 스피커 TTS |
| M3 v34 병합 | `feature/ast-base` → develop(`cb0ea6f`). 계약 검증 PASS, 낙상 50회 재현 **46/50** |
| CPU·GPU 비교 | 정확도 동일, 속도는 GPU가 2~150배 빠름(아래 표) |
| 경계값·Phase 2 | 경계값 18/18, Phase 2 + TTS 3/3(CPU·GPU) |
| 빈 방 장시간 | 113분 기록(엑셀 113개, 5.1 GB), 재시작·오류 0, **M1 오경보 65건(시간당 약 35건)** |
| 고친 것 | GPU 실행 경로(CUDA 12.8, M4 GPU), M2 off 시 `심박:0` 오판, M5 점수 예비 추출, M5 조용한 fallback |

| | RPi5(9/17) | 노트북 CPU | 노트북 GPU |
|---|---|---|---|
| M3 1회 | 옛 모델 6.3 s / v34 미측정 | 0.61~2.15 s | 0.04 s |
| M4 1회 | 4.43 s | 0.65~1.00 s | 0.47 s |
| 음성 → 결과 | 최대 17 s | 1.3~3.0 s | 0.75 s |
| M5 1회 | 9~23 s | 1.6 s | 0.75 s |
| M4 정확도(200) | — | CER 5.15%, 키워드 90% | 같음 |
| M5 정확도(100) | — | exact 51%, adj 82%, 놓침 0 | exact 53%, adj 82%, 놓침 0 |

**판단**: 소프트웨어는 죽거나 갇히지 않고, GPU급 장비에서는 실시간 여유가 충분하다. 운용을 막는 것은 **M1 빈 방 오경보**(경보·TTS·M5가 모두 오경보로 채워짐)다. 운용 가능성은 영상 기반 시나리오 시험(감지율·오경보/일)으로 판단한다.

## 2. 팀원별 할 일
### 김태연 (M1·M2) — `TO_KIMTAEYEON_M1_M2.md`
1. 빈 방 오경보 원인 의견: 학습 때 빈 방 표본·배치 차이
2. 입력 격자: 수신 시각 대신 장치 시각(`ts_ms`)·순번 기준으로 바꿔도 되는지
3. raw 채널 0(노드 2의 59% 행, 채널 37·53·45·29 등)이 학습 데이터에도 있었는지
4. 노드 4·5 사용 방법(재학습 필요 여부)
5. 영상 촬영 때 필요한 동작 목록·횟수·배치 조건 → 라벨 CSI로 재학습
6. M2 실모델 일정(그전까지 off)

### 소민섭 (M3, 보호자 앱) — `TO_SOMINSEOP_M3.md`
1. RPi5 권장 스레드 수, 모델 메모리(0.57 GB 기준)
2. VAD -55 dB 근거(병합 때 -45 유지)
3. `results.csv`가 이전 모델 결과인지, v34 재채점본
4. 실험실 충격음 실측(영상 촬영 때) 조건
5. (앱) 서버 쪽 변경 없음. S2 확인 기능·S6 인증은 여전히 결정 대기

### 이대경 (M4) — `TO_LEEDAEGYEONG_M4.md`
1. 환각 대응 의견: 알려진 문장 제거, 짧고 작은 조각 버리기, 반복 방지 guard 통합
2. sensing의 조용한 이벤트 증폭(peak 0.85)이 환각에 주는 영향
3. "넘어졌어요" → "나마졌어요" 오인식
4. GPU용 FP16 모델 필요 여부

### 노진산 (통합·M5) — `M5_NOTES_NOJINSAN.md`
| 순위 | 할 일 | 비고 |
|---|---|---|
| 1 | M5 긴급 키워드 가드(키워드 있으면 최소 warning) | "낙상위험 51~59% + 도와주세요 → normal" 발견 |
| 2 | RPi5 반영: pull, **M3 모델 설치**(`setup_m3_ast_onnx.py`), M3 스레드 1·2·4 측정, ai-experts 메모리(노트북 2.71/3 GB) 확인 | M3 v34 RPi5 미측정 |
| 3 | 영상 시험 준비: 화면 시계 페이지, 경보 내보내기, 채점 스크립트, 시나리오표 | 운용 가능성 판단의 핵심 |
| 4 | M3 ambient 4시간 오탐률 재현(GPU 약 10분) | 담당자 채점 CSV와 대조 |
| 5 | M5 평가 13건 정답 재판정, reason "미측정" 정리, `n_threads_batch` 설정 | |
| 6 | 규칙 경보 뒤 M5 억제 설계 결정, FCM 키 받아 실제 수신 확인 | |
| 7 | CSI zip(5.4 GB) 구글 드라이브 업로드 → 링크를 김태연 문서·노션에 | `data/csi/SafeWave_CSI_empty_room_20260923.zip` |
| 8 | 정리: GPU 이미지 오늘 수정분 리빌드, 테스트 순서(가짜 utils) 정리, 휴지통의 23:55~57 파일 | |

## 3. 데이터 위치 (노트북, Git 제외)
| 데이터 | 위치 |
|---|---|
| 빈 방 CSI 엑셀 113개 / zip | `data/csi/excel/`, `data/csi/SafeWave_CSI_empty_room_20260923.zip` |
| M3 평가셋 | `data/m3_eval_v34/`, 원본 zip `data/safewave_eval_*.zip` |
| 실험 로그·결과 | `reports/laptop/` (bench, m4eval, m5eval, voice_probe, slm_probe, phase2) |
