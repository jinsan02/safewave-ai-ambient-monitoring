# 고려사항 — 결정 대기 목록 (2026-09-17)

코드 점검(서브에이전트 사람 관점·파이프라인·모니터링)과 노트북 전체 테스트에서 나온 항목 중
**제품·정책 결정이 먼저 필요한 것**만 모았다. 결정 전에는 구현하지 않는다. 결정 뒤 이 표에 결과를 적는다.

| # | 영역 | 내용 | 근거 | 선택지 | 결정 |
|---|---|---|---|---|---|
| 1 | 판단 | M5가 규칙 점수 0.09~0.13 입력을 critical(1.0)로 올림(노트북, 낙상 발화 음성) | `qwen_service` 로그 `gate_risk_score`→`risk_score` | M5 단독 상향 폭 상한 / 키워드 동반 시만 허용 / 현행 유지 | |
| 2 | 안전 | 응답 없음·도움 요청 후 추가 조치 없음 | `api/main.py` Phase 2는 "괜찮다"만 후속 알림 | 재알림 주기, 보호자 확인(ack) API, 다음 연락처, 119 연계 | |
| 3 | 안전 | 시스템·노드 정지를 보호자가 모름 | 노드 오프라인은 대시보드에만 표시, M1은 노드 1·2·3이 모두 있어야 동작 | 노드 N분 오프라인 알림, 일일 "정상 동작" 알림, 외부 watchdog | |
| 4 | 안전 | 설정 화면에서 누구나 AI·M1을 끌 수 있음 | `monitor.html` 모델 토글, `POST /settings` 인증 없음 | 확인 창, 끄면 보호자 알림, 관리자 인증 | |
| 5 | 보안 | API 인증 없음, CORS `*` | `api/main.py` 미들웨어, `/notify/send`로 임의 푸시 가능 | API 키, CORS 제한, `/notify/send` 제거 | |
| 6 | 보안 | MQTT 익명 접속 허용(integration/home 프로필) | `mosquitto.conf` | 비밀번호 파일 | |
| 7 | 보안 | Redis 비밀번호 없음(포트는 127.0.0.1로 제한함) | `db/redis.conf` | `requirepass` 추가(모든 서비스 설정 변경) | |
| 8 | 개인정보 | 마이크 상시 녹음, 음성 원본 최근 120건 보관, 동의·음소거 절차 없음 | `sensing/audio_main.py` | STT 후 원본 즉시 삭제, 보관 기간 명시, 물리 음소거·LED, 동의 절차 | |
| 9 | 개인정보 | transcript가 `ai:result`·`/logs`·웹소켓으로 인증 없이 노출 | `api/main.py` | 권한 있는 요청에만 포함 | |
| 10 | 사용성 | TTS가 인터넷(edge-tts)에 의존, 안내 문구가 기계적·해제 방법 안내 없음 | `scripts/tts_worker.py` | 로컬 WAV, 문구 개선(에코 키워드 제약 고려), 반복·음량 | |
| 11 | 사용성 | 야간 반복 안내, 경고마다 발화(MQTT 경로) | `tts_worker.py` 120초 쿨다운 | critical만 발화, 시간당 상한, 야간 모드 | |
| 12 | 사용성 | 알림 기기 ID 기본값 공유(`galaxy_flip4`) → 두 번째 보호자가 덮어씀 | `TokenRegistration` | 고유 ID 필수, 다중 보호자 문서화 | |
| 13 | 사용성 | 노드 번호만 표시, 방 이름 없음 | 알림 본문 `[노드 N]` | 설정에 노드→방 이름 매핑 | |
| 14 | 운영 | 경보 후 90초 동안 같은 노드 두 번째 사건 미평가 | `phase2:active` 락(API·규칙 경보·M5 공통) | 현행 유지 / 락 정책 전체 변경 | 현행 유지(09-17) |
| 15 | 운영 | 설정 동시 수정 시 덮어씀 | `POST /settings` 전체 교체 | 부분 수정 API | |
| 16 | 운영 | 대시보드 좁은 화면에서 카드·범례 잘림 | 노트북 패널 확인 | 반응형 수정 | |
| 17 | 모델 | M1 규칙 경보 오알림률 미확인(합성 입력에도 3/5 양성) | 노트북 테스트 | 빈 방 1시간 실측 전 규칙 경보 유지 / 보호자 알림을 M5 확인 후로 | |
| 18 | 운영(노트북) | Docker 소켓 파일 접근 불가(Win32 1920) 근본 원인 | `scripts/dev/start_docker_desktop.ps1`로 우회 중 | 관리자 `fltmc filters`로 필터 드라이버 확인 | |

이미 반영한 것(참고): FCM 토큰·설정 TTL 연장, 단일 마이크 노드, 도움 요청 오분류, 후속 알림 분리,
알림 본문 노드·사유, 대화 내용 로그 제거, Redis 로컬 바인딩, Firebase 미설정 경고.
